# 웹사이트 배포 가이드

Dentric 웹사이트는 `HTML`, `CSS`, `JS`로 이루어진 정적(Static) 웹사이트입니다.
서버 비용 없이 무료로, 그리고 아주 빠르게 배포할 수 있는 가장 좋은 방법 3가지를 추천해 드립니다.

---

## 🚀 추천 1: Netlify (가장 추천)
**장점:** 사용이 가장 쉽고(폴더 드래그 앤 드롭), 설정이 간편하며, HTTPS가 자동으로 적용됩니다.

### 배포 방법 (5분 컷)
1. [Netlify](https://www.netlify.com/) 회원가입 및 로그인
2. 로그인 후 대시보드 화면으로 이동
3. 탐색기에서 `marketing-web` 폴더 자체를 마우스로 끌어서 브라우저 화면(Netlify 대시보드)에 **드래그 앤 드롭**
4. 배포 끝! 랜덤한 주소(예: `brave-dentric-12345.netlify.app`)가 생성됩니다.

### 도메인 연결 (dentric.co.kr)
1. Netlify 대시보드에서 **"Domain settings"** 클릭
2. **"Add custom domain"** 클릭하고 도메인 입력
3. 도메인 구입처(가비아, 호스팅케이알 등)에서 DNS 설정(CNAME, A 레코드) 변경 (Netlify가 안내해 줌)

---

## ⚡ 추천 2: Vercel
**장점:** 한국에서도 속도가 매우 빠르고, 추후 Next.js 등으로 고도화할 때 유리합니다.

### 배포 방법
1. [Vercel](https://vercel.com/) 회원가입 (GitHub 아이디로 가입 추천)
2. Vercel CLI 설치 (`npm i -g vercel`) 후 터미널에서 `vercel` 입력하거나,
3. 또는 GitHub에 코드를 올린 후 Vercel 대시보드에서 **"Add New Project"** -> GitHub 레포지토리 선택
4. **Deploy** 버튼 클릭하면 끝.

---

## 🐙 추천 3: GitHub Pages
**장점:** 개발자들에게 가장 익숙하고 완전 무료입니다.

### 배포 방법
1. GitHub에 새로운 Repository 생성 (예: `marketing-web`)
2. 코드 업로드 (Push)
3. Repository 설정(Settings) -> **Pages** 메뉴 이동
4. **Branch**를 `main`으로 설정하고 **Save**
5. `https://아이디.github.io/marketing-web` 주소로 배포됨

---

## 💡 결론
*   **가장 쉽고 빠른 방법:** 👉 **Netlify** (드래그 앤 드롭만 하면 끝)
*   **성능과 확장성 고려:** 👉 **Vercel**
*   **GitHub을 이미 쓰고 있다면:** 👉 **GitHub Pages**
